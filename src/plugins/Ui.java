package plugins;
import Core.*;

public class Ui {
		private Loader loader;
	
	public Ui(){
		loader = Loader.getInstance();
	}
	
	
	public void  getPlugins(Object o){
		//System.out.println(this.getClass().getClassLoader().getClass().getName());
		//(o)loader.loadObject(o);
	}
	
	//some property
}
